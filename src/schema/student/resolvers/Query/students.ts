import type { QueryResolvers } from './../../../types.generated'
export const students: NonNullable<QueryResolvers['students']> = async (
  _parent,
  _arg,
  _ctx,
) => {
  /* Implement Query.students resolver logic here */
}

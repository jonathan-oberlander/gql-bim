import type { QueryResolvers } from './../../../types.generated'
export const instructors: NonNullable<QueryResolvers['instructors']> = async (
  _parent,
  _arg,
  _ctx,
) => {
  /* Implement Query.instructors resolver logic here */
}

import type { QueryResolvers } from './../../../types.generated'
export const instructor: NonNullable<QueryResolvers['instructor']> = async (
  _parent,
  _arg,
  _ctx,
) => {
  /* Implement Query.instructor resolver logic here */
}

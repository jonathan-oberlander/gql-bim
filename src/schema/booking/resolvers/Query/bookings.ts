import type { QueryResolvers } from './../../../types.generated'
export const bookings: NonNullable<QueryResolvers['bookings']> = async (
  _parent,
  _arg,
  _ctx,
) => {
  /* Implement Query.bookings resolver logic here */
}
